<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тёмная еловая тема: полоса статуса сливается с интерфейсом приложения. -->
    <style name="Theme.MenuLiveWaiter" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/ml_ground</item>
        <item name="android:statusBarColor">@color/ml_ground</item>
        <item name="android:navigationBarColor">@color/ml_ground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
